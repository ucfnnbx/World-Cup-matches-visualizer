using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class myDisplayScript : MonoBehaviour
{

    public mqttControllerList MyController;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
//     void Update()
//     {
        
//         if(MyController.convertObject != null && MyController.convertObject.home_score != null){
//             Debug.Log("This is from another Obj"+MyController.convertObject.home_score);

//         }

//     }
 }
